---
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;

---

<Layout title={frontmatter.title}>
    <article class="mx-auto prose prose-lg lg:prose-xl xl:prose-2xl dark:prose-invert mt-4">
        <slot />
    </article>
</Layout>